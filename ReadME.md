*Fashion Item Detector Application*
1. First you need to Install required libraries by pip install -r requiremnts.txt
2. Then Run main.py to train the models
3. Finally run streamlit run app.py to view the web applications.
